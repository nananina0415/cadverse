# AGENTS — 이 프로젝트에서 AI에게 요구하는 작업 지침

이 프로젝트는 여러 팀원이 각자 다른 AI 채팅을 통해 코드를 작성한다.
브랜치 간 파편화·중복 구현·스키마 불일치로 통합이 어려워진 경험을 바탕으로,
**모든 AI가 작업 전에 이 문서를 따른다**고 가정한다.

핵심: AI는 자율 실행이 아니라 채팅에서 사용자에게 제안하는 입장이다.
따라서 잠재 위험은 **AI가 먼저 명시적으로 알려야** 한다.

---

## 1. 프로젝트 폴더 위반 금지

- 새 폴더(예: `sim_server/`, `new_module/`)를 임의로 만들지 말 것
- 같은 책임의 기존 폴더가 있으면 그쪽에 추가
- 새 폴더가 정말 필요하면 작성 전에 사용자에게 위치/이름을 확인

## 2. 레이어 위반 금지

- UI가 네트워크/P2P를 직접 다루거나, 시뮬레이터가 클라 좌표계를 알고 있는 등 책임 침범 금지
- 각 레이어는 그 위·아래 레이어가 처리할 수 없는 일만 담당 (`CLAUDE.md` 참고)
- "이건 어느 레이어에 들어가야 하지?"가 모호하면 작성 전에 확인

## 3. 하드코딩 금지

- 모델 이름, 부품 인덱스, 경로, 매직 넘버 등 특정 환경에 종속되는 값을 코드에 박지 말 것
- 메타데이터·설정 파일에서 읽거나, 인자로 받기
- 테스트 모델 한 종류에만 통하는 dict(`{"shaft": 0, "base": 1, ...}` 같은 것)는 명백한 안티패턴

## 4. 기존 코드/패턴 우선

- 새 함수/모듈을 만들기 전에 같은 책임의 기존 코드가 있는지 grep
- 같은 변환을 다른 곳에서 또 작성하지 말 것 (좌표 변환을 인라인으로 또 짜는 등)
- 이미 있는 함수/유틸을 재사용. 부족하면 그 함수를 확장

## 5. 기능 단위 브랜치 + 안전한 머지

**원칙**

- 한 브랜치 = 한 기능. 여러 기능을 한 브랜치에 쌓지 말 것
- 기능 구현 → 테스트 → 메인 머지 → 다시 새 브랜치

**기능 간 선후관계가 있을 때**

- 선행 기능을 먼저 완성하고, 그 브랜치에서 새 브랜치를 뻗거나
- 선행 기능을 메인에 머지한 뒤 메인에서 새 브랜치를 뻗는다

**메인 머지 방식**

- 현재 최신 origin/main 위에 `merge/<feat>` 브랜치를 새로 만들어 그 브랜치로 변경을 옮긴다
- 옮기는 방식(squash, rebase, merge)은 자유. 핵심은 **충돌 해결을 작업 브랜치가 아닌 머지 전용 브랜치에서** 한다는 점
- 충돌 해결 후 그 머지 브랜치를 메인에 머지

## 6. 변경 전 영향 범위 추적 + 사전 공유

- 함수 시그니처, JSON/protobuf 스키마, 공유 enum, 메타데이터 키 등 **다른 모듈/브랜치가 의존하는 인터페이스**를 변경할 때
  - 모든 호출처/사용처를 추적 (grep으로 충분)
  - **변경 전에 팀원들과 공유**. 단순 필드 추가도 다른 측에서 무시되거나 에러를 던질 수 있다
- 변경하면 그 영향을 받는 다른 코드까지 **변경자가 책임지고** 같이 수정
- 변환·인코딩에는 **양방향 일관성** 유지 (Recv를 만들었으면 Send도, LH→RH 규칙이 있으면 RH→LH도)

## 7. 자율 실행 가정 금지

- AI는 변경을 제안하고, 사용자가 직접 실행/커밋/머지/테스트한다
- "내가 머지해 둘게" 같은 자율 실행을 가정하지 말 것
- 사용자에게 **무엇을 어디서 어떻게 실행해야 하는지** 명확히 안내

## 8. 같은 문제는 한 곳에서

- 버그가 발생하면 **다른 팀원이 이미 해결 중인지** 먼저 파악 (이슈 트래커, 다른 브랜치, 슬랙 등)
- 다른 팀원이 해결 중이면, 직접 fix하지 말고 **그 fix가 시작된 이전 커밋에서 새 브랜치를 만들어** 본인 작업을 진행. 머지될 fix를 기다림
- 각 브랜치에서 같은 버그를 다르게 고쳐 합치는 게 가장 큰 파편화 원인

## 9. 비공식/비표준 의존성 회피

- 비공식 conda env(예: `chrono_310`), 별도 venv, 비표준 패키지 경로 등을 명시 합의 없이 도입 금지
- 새 라이브러리/패키지 추가 전에 기존 대안 검토
- 의존성을 추가하면 그 이유를 사용자에게 설명

## 10. 노이즈 변경 금지

- 관심 작업과 무관한 변경을 같이 끼워 넣지 말 것
  - 로그/에러 메시지 일괄 제거
  - 코드 포매팅 일괄 변경
  - 관련 없는 함수의 리네임
- "이 줄은 왜 있지?" 모르면 그대로 두기. 의도 모를 코드를 임의로 삭제 금지

## 11. 단위/좌표/의미 표기 정확성

- `FUSION_CM_TO_M` 같은 단위 상수는 의미를 알기 전에 손대지 말 것. 잘못된 단위(mm vs cm)나 좌표계(LH vs RH)로 되돌리지 말 것
- 좌표계·축 매핑·손방향·단위는 함수명 또는 주석에 명시
- 변환 함수에 "정확히 무엇이 무엇으로 변환되는지" 한 줄 주석

## 12. 충돌 해결 시 양쪽 의도 파악

- merge conflict에서 한쪽 코드를 임의로 선택하지 말 것
- 양쪽 변경이 왜 도입됐는지 commit log·diff로 의도 파악
- 의도가 충돌하면 사용자에게 확인. 한쪽 의도를 무시하면 그쪽 작업자의 변경이 사라진다

---

## AI에게 요구하는 행동 패턴 (요약)

- 작업 전: 영향 범위 파악, 기존 패턴 검토, 위험 사전 공지
- 작업 중: 한 가지에 집중, 노이즈 변경 X, 의미 모르는 코드 보존
- 작업 후: 무엇을 어떻게 실행할지 안내, 변경 요약 명시

사용자가 모를 수 있는 잠재 문제를 발견하면 **AI가 먼저 명시적으로 경고**한다.
"이대로 진행하면 X가 깨질 가능성이 있어 보입니다. 먼저 확인이 필요합니다." 같은 한 마디가 AI의 핵심 역할.
